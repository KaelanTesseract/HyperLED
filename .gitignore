.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch

# Separate PlatformIO project with its own repo (KaelanTesseract/HyperLED-Slave) -
# never track it here, this repo is Master-only.
HyperLED_Slave/

# Android companion app - separate project, not part of this firmware repo.
HyperLED_Android/

# Frozen historical snapshot, not live code.
archive/

# Built LittleFS web UI assets - data/*.js, *.html, *.css are the tracked sources;
# these gzipped copies are build output (see CLAUDE.md's uploadfs workflow).
data/*.gz

# Local tooling config, not part of the shared repo.
.claude/
.mcp.json

# Scratch/dev-testing scripts, not firmware source.
test.py
test2.py

# Personal feature backlog, kept local.
Ideen.md

__pycache__/
